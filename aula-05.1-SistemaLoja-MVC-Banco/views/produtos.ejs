<%- include ('partials/header.ejs') %>
<%- include ('partials/navbar.ejs') %>

<div class="container">
    <div class="card">
        <div class="card-header">
            <h1>Cadastrar produtos</h1>
        </div>
 
    <div class="card-body">
        <form action="/produtos/new" method="post">
            <label>Nome do Produto:</label>
            <input type="text" name="nome" id="nome" placeholder="Insira o nome do cliente" class="form-control" required>
 
            <label>Valor:</label>
            <input type="text" name="cpf" id="cpf" placeholder="Insira o CPF do cliente" class="form-control" required>
 
            <label>Categoria:</label>
            <input type="text" name="endereco" id="endereco" placeholder="Insira o endereço do cliente" class="form-control" required>
            <br>
            <input type="submit" class="btn btn-success" value="Cadastrar">
        </form>
        <hr>
    </div>
 </div>
</div>  

<div class="container">
        <div class="card">
            <div class="card-header">
                <h1>Produtos</h1>
            </div>
            <div class="card-body">
                <table>
                    <thead>
                        <tr>
                            <th></th>
                            <th>Nome:</th>
                            <th>Preço:</th>
                            <th>Categoria:</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% produtos.forEach(produto => { %>
                        <tr>
                            <td><img src="imgs/product.png" width="30px"></td>
                            <td><%= produto.nome %></td>
                            <td><%= produto.preco.toLocaleString('pt-br', {style: 'currency', currency: 'BRL'}) %></td>
                            <td><%= produto.categoria %></td>
                            <td> <a style="color: orange;"" href="/produtos/edit/<%= produto.id %>"> Editar </a></td>
                            <td> <a style="color: red;"" href="/produtos/delete/<%= produto.id %>"> Deletar </a></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

<%- include ('partials/footer.ejs') %>
